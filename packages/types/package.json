{
  "name": "@modulus/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types and MSW handlers for Modulus",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@modulus/config": "*",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "msw": "^2.7.0",
    "zod": "^3.24.1"
  }
}
